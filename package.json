{
  "private": true,
  "scripts": {
    "start": "yarn workspace react-impression start",
    "precommit": "lerna run --concurrency 1 --stream precommit"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.11.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
